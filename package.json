{
  "name": "@konstant/payload-client",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsup",
    "lint": "eslint . --ext .ts",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "payload": "^2",
    "pino": "^9.4.0",
    "qs": "^6.13.0",
    "ts-essentials": "^10.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@ianvs/prettier-plugin-sort-imports": "^4.3.1",
    "@types/qs": "^6.9.16",
    "prettier": "^3.3.3",
    "tsup": "^8.3.0",
    "typescript": "^5"
  }
}
